<div class="container">
<%= simple_form_for @debt, url: wizard_path, method: :put do |f| %>
<%= f.input :name, label: "Name" %>
 <%= f.submit "Create Debt Profile", class: 'btn btn-primary' %>
<% end %>
<%= link_to 'skip', next_wizard_path %>
</div>
